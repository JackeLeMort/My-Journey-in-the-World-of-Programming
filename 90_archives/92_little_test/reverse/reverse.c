#include <cs50.h>
#include <stdio.h>

int main(void)
{
    char one();
    char two();
    one = get_char("première lettre");
    two = get_char("deuxième lettre");

    printf(two);
    printf(one) ;
}